pipeline {
    agent any 

    stages {
        stage('Hello Stage') {
            steps {
                sh 'javac Addition.java'
            }
        }
        stage('Run'){
            steps{
                sh 'java Addition'
    }
}
    }
}
